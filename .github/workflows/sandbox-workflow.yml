name: sandbox workflow
on: 
  push:
    branches: [ main ]
  workflow_dispatch:
  
env:
  ENV_NAME: 'staging'


jobs:
  job1:
    runs-on: ubuntu-latest
    name: $ENV_NAME
    environment:
      name: staging2
    steps:
      - run: echo "hello $ENV_NAME"
